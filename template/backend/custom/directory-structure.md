# 目录结构

> 本项目后端代码的组织方式。

---

## 概览

> 说明当前项目后端目录结构的整体设计思路。
> 说明目录结构应如何服从真实仓库布局，而不是凭空发明层级。
> 说明目录结构与模块职责、部署边界、代码搜索效率之间的关系。

---

## 目录布局

> 列出项目后端目录结构示例，并说明各目录用途。
> 说明源码目录、测试目录、资源目录、脚本目录、文档目录等如何划分。
> 如果存在多模块或多服务目录，说明顶层目录之间的职责边界。

---

## 模块组织

> 说明项目如何按模块、子系统或业务域拆分。
> 说明模块划分依据是业务域、技术边界、部署单元还是团队协作边界。
> 如果没有模块化拆分，也应说明当前单体结构下的组织原则。

### API 入口模块

> 说明控制器、协议适配、参数接收、响应转换等内容应放在哪里。
> 说明这一层允许做什么、不允许做什么。

### 核心业务模块

> 说明核心业务规则、用例编排、事务边界、领域服务应放在哪里。
> 说明业务模块之间如何保持边界稳定，避免互相打穿。

### 共享领域模块

> 说明哪些对象、规则或能力可以沉淀为共享模块。
> 说明共享模块的复用边界，避免把一切都塞进 common。

### 集成与协议模块

> 说明数据库、缓存、消息、外部 HTTP/SDK、文件系统等集成代码应放在哪里。
> 说明协议适配、基础设施实现与业务代码的隔离方式。

---

## 包结构约定

> 说明包名、目录名、模块名之间应如何对应。
> 说明包结构是按业务域、技术层还是组合方式组织。
> 说明如何避免循环依赖、跨层直连和职责重叠。

---

## 命名约定

> 说明目录、模块、包、子包的命名规则。
> 说明是否允许缩写、拼音、历史别名或技术名词混入业务目录。
> 说明测试目录、脚本目录、临时目录的命名约束。

---

## 参考示例

> 给出 1 到 3 个符合当前项目约定的目录结构片段示例。
> 对每个示例补充“该结构适用于什么场景”的说明。

---

## 反模式

> 列出当前项目最应避免的目录结构反模式。
> 说明哪些拆分方式会导致职责混乱、复用失控或排障困难。
> 如果历史上踩过坑，可记录“错误结构 + 正确替代方式”。