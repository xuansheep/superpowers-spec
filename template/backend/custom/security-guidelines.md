# 安全规范

> 面向项目的安全基线规则，聚焦输入校验、认证授权、敏感信息处理、错误暴露控制与常见风险规避。

---

## 概览

安全规范不是给审计看的门面文件，而是日常开发必须兑现的底线。

接口、文件、SQL、日志、配置、回调、鉴权这些环节，只要有一个地方脑子一热图省事，就可能把整个系统拖进坑里。安全问题不会因为“这是内部系统”就自动消失，这种侥幸心态本身就是漏洞温床。

---

## 输入校验与边界检查

> 对于外部输入的类型、格式、长度、范围、白名单与非法输入处理要求的定义。
> 对于嵌套对象、集合、批量请求、文件参数、路径参数、动态字段等校验要求的定义。
> 对于校验应放在哪一层、失败应如何响应、是否允许自动纠正输入的说明。

---

## 认证与授权

> 对于认证机制、会话机制、令牌机制或签名机制的定义。
> 对于授权入口、最小权限原则、资源归属校验、多租户隔离要求的定义。
> 对于高风险操作、后台管理操作、跨组织访问场景的额外约束说明。

---

## 敏感信息处理

> 对于密码、Token、密钥、个人信息、内部地址等敏感信息的识别范围定义。
> 对于存储、传输、脱敏、加密、展示、导出、日志记录方式的定义。
> 对于配置文件、环境变量、示例代码、测试数据中敏感信息处理的约束说明。

---

## 日志与错误暴露控制

> 对于对外错误信息应暴露到什么程度、哪些细节禁止泄露的定义。
> 对于对内日志如何保留排障上下文、如何避免敏感数据落日志的定义。
> 对于审计日志、安全日志、异常日志的保留要求说明。

---

## 数据存储与传输安全

> 对于敏感数据落库、脱敏、加密、哈希、分级存储策略的定义。
> 对于外部传输协议、证书校验、随机值生成、配置访问控制的定义。
> 对于第三方系统交互、密钥轮换、失效策略、证书管理的说明。

---

## 文件与外部资源安全

> 对于文件上传、下载、预览、导出、删除等操作的安全要求定义。
> 对于路径拼接、外部 URL、回调地址、脚本执行、命令执行边界的定义。
> 对于压缩包、模板、富文本、可执行文件、第三方资源访问的约束说明。

---

## 常见高风险模式

> 列出当前项目重点关注的高风险模式，如注入、越权、信息泄露、路径穿越、反序列化等。
> 对每一种风险说明触发条件、影响范围和最低防护要求。

---

## 常见错误

> 列出当前项目常见的安全错误示例。
> 说明哪些看似省事的写法会直接引入漏洞或审计风险。
> 如果有历史事故或高风险案例，可补充“错误模式 + 防范要求”。